import 'package:flutter/material.dart';

// Fungsi stub untuk non-web platform
void registerWebView(String viewId, String url) {
  // Tidak melakukan apa-apa di mobile
}

Widget getWebView(String viewId) {
  return const Center(child: Text("Platform tidak didukung"));
}
